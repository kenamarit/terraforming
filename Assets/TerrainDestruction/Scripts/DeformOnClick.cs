using UnityEngine;
using System.Collections;

public class DeformOnClick : MonoBehaviour {
/*
	TerrainDeformer deformer;
	
	void Start () {
		deformer = GetComponent<TerrainDeformer>();
	}
	
	
	
	// Update is called once per frame
	void Update () {
		
		Vector3 coord = Camera.main.transform.GetComponent<KinectOSC>().coordinates;
		Vector3 handScreenPos = Camera.main.WorldToScreenPoint(coord);
		
		if(Input.GetMouseButtonDown(0)) {
			RaycastHit hit;
			if(Physics.Raycast(Camera.main.ScreenPointToRay(handScreenPos), out hit, 1000)) {
				deformer.Damage(hit.point, coord.z / 10);
			}
		}
	}
	*/
}
